<article class="TextBody body-text">
  <slot />
</article>

<style is:global lang="scss">
  .TextBody {
    width: 100%;
    padding-bottom: 3rem;
    counter-reset: footnotes;

    & > *,
    & > .passthrough > * {
      max-width: calc(var(--max-content-width) + 2 * var(--page-padding));
      margin-left: auto;
      margin-right: auto;
      padding: 0 var(--page-padding);
    }

    & h1 {
      font-size: 3.75rem;
      margin-top: 1em;
      margin-bottom: 3.5em;
      text-transform: uppercase;
    }

    & h2 {
      font-size: 3.75rem;
      text-align: center;
      margin-top: 1.1em;
      margin-bottom: var(--page-padding);
      text-transform: uppercase;
    }

    & p {
      font-size: 1.25rem;
    }
  }
</style>
