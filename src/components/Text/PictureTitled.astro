---
import Carousel from "@/components/Modules/Carousel.astro";
import type { PictureTitled } from "@/sanity";
import type { Block, Props as $ } from "astro-portabletext/types";

export type Props = $<PictureTitled>;

const {
  props: { node },
} = Astro;
---

<div>
  <Carousel value={[node]} id={(node as any)._id} />
</div>
