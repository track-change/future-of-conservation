---
import { PortableText } from "astro-portabletext";
import type { EditorText, EditorTextMedia } from "@/sanity";

interface Props {
  content: EditorText | EditorTextMedia;
}

const { content } = Astro.props;
---

<PortableText value={content} />
